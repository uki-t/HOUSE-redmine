---
description: Redmineのチケット一覧を取得して表示する
---

以下の手順でRedmineのチケット一覧を取得してください：

1. `.env` を読み込む
2. `/issues.json` を呼び出す（`status_id=*` で全ステータス取得）
3. チケット番号・件名・ステータス・担当者・優先度を表にまとめて表示する

追加の絞り込み条件があれば適宜クエリパラメータを追加してください。
